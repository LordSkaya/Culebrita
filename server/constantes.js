/// Tasa de fotogramas y tamaño de la pantalla, 
//para que cualquiera en el server pueda usarlos

const Frame_rate = 10;
const tamaño_grid = 20;

module.exports = {

    Frame_rate,
    tamaño_grid,
}